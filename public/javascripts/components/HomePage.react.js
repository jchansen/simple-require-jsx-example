define(function(require) {

  var React = require('react');

  return React.createClass({

    render: function () {
      return (
        <div>
          <h1>Express</h1>
          <p>Welcome to the RequireJS+JSX Example</p>
        </div>
      );
    }

  });

});
